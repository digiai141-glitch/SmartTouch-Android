This directory holds signing keystores.

For development, Android Studio generates a debug keystore at:
  ~/.android/debug.keystore

For release builds, supply these environment variables (see README):
  KEYSTORE_PATH
  KEYSTORE_PASSWORD
  KEY_ALIAS
  KEY_PASSWORD

NEVER commit real keystores to source control.
The .gitignore already excludes *.keystore and *.jks.
